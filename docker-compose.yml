services:
  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    volumes:
      - .//ap/src:/app/src:ro
      - .//ap/index.html:/app/index.html:ro
      - .//ap/public/inject.js:/app/public/inject.js:ro
      - .//ap/vite.config.ts:/app/vite.config.ts:ro
      - .//ap/tailwind.config.js:/app/tailwind.config.js:ro
      - .//ap/postcss.config.js:/app/postcss.config.js:ro
      - .//ap/.env:/app/.env:ro
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
    ports:
      - "6903:6902"
    command: npm run dev
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    volumes:
      - ./frontend/src:/app/src:ro
      - ./frontend/index.html:/app/index.html:ro
      - ./frontend/public/inject.js:/app/public/inject.js:ro
      - ./frontend/vite.config.ts:/app/vite.config.ts:ro
      - ./frontend/tailwind.config.js:/app/tailwind.config.js:ro
      - ./frontend/postcss.config.js:/app/postcss.config.js:ro
      - ./frontend/.env:/app/.env:ro
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
    ports:
      - "6902:6902"
    command: npm run dev
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"
